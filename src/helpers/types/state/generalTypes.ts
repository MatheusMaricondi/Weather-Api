type generalType = {
    loading: boolean,
    language: string
}

type citiesType = {
    country: string,
    name: string,
    lat: string,
    lng: string
}

export type { generalType, citiesType } 