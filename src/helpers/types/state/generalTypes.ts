type generalType = {
    loading: boolean,
    language: string
}

type citiesType = {
    country: string,
    name: string,
    lat: number,
    lng: number
}

export type { generalType, citiesType } 